# Deora Concept

Static website for Deora Concept coworking space.

## Structure
- `index.html` — the site (open in any browser)
- `assets/` — logo, favicon, and photos

## Deploy (GitHub Pages)
Push to a repo, then enable Pages: Settings > Pages > Branch: `main` / root. Site serves `index.html` automatically.
